import theme, {
	COLORS,
	SIZES,
	// FONTS,
	darkTheme,
	lightTheme,
	selectedTheme,
} from './theme';

const isSmall = SIZES.height < 720;

export {
	theme,
	COLORS,
	SIZES,
	// FONTS,
	darkTheme,
	lightTheme,
	selectedTheme,
	isSmall,
};
